# agents-and-workflows

A repository for testing various AI artifacts and scripts.

## Routing Copilot to Alternate Inference Providers

Copilot CLI uses the following environment variables to overwrite its own internal values:

- `COPILOT_PROVIDER_BASE_URL`
- `COPILOT_PROVIDER_API_KEY`
- `COPILOT_MODEL`
